# グローバル開発ルール

## セキュリティ（最優先）
- `.env`、`.env.*`、`*.pem`、`*.key` の読み取り禁止
- `~/.ssh/`、`~/.aws/`、`~/.gnupg/` へのアクセス禁止
- `curl`、`wget`、`nc` による外部通信禁止
- `rm -rf`、`chmod 777`、`git push --force`、`git reset --hard` 実行禁止
- APIキー・パスワードのハードコード禁止
- 機密情報のログ・コンソール出力禁止

## 開発ワークフロー
1. 作業開始前にパーミッション設定を確認する
2. 実装前にテストコードを作成する（TDD）
3. テストをパスし、禁止ルールに抵触しないコードのみ生成する
4. トークン40〜50%超過時は進捗をファイルに書き出し `/clear` を提案する
5. 完了時にセキュリティチェック済みの旨を日本語で報告する

## コード品質
- エラーハンドリングは必ず実装する
- 未使用のインポートや変数を残さない
- コメントは「なぜ」を書く。「何を」はコードで表現する
- マジックナンバーは定数化する

## コミュニケーション
- エラー説明・提案・報告はすべて日本語で行う
- 権限エラー発生時はどのルールに抵触したか具体的に説明する
- `git push` はユーザーに任せる。自動実行しない
