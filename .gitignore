# 環境変数・機密情報
.env
.env.*
.env.example
*.pem
*.key

# Claude Code 個人設定（チームに共有しない）
CLAUDE.local.md
.claude/settings.local.json

# 作業指示のメモ（成果物だけをリポジトリに残す）
td.md
feed.md

# 依存パッケージ・ビルド成果物（package-lock.jsonから再現可能）
node_modules/
dist/
output/

# macOS
.DS_Store
