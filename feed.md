現素材からのプログラムによる透過処理は中止してください。

今回は、すべてを透過PNGへ変換するのではなく、以下のハイブリッド方式で進めます。

■ 純黒背景で再出力し、CSSのmix-blend-mode: screenで合成する素材
- 2-2 ポータルリング
- 2-3 ポータル内部光
- 2-4 軌道線・粒子
- 2-6 UIパネル
- 2-7 UIパネル
- 2-8 球体・光球

■ 本物の透過画像として用意する素材
- 2-5 台座

■ 通常の不透明背景として使用する素材
- 2-9 背景・床反射

ポータル・軌道線・粒子・球体などの発光素材は、背景を完全な純黒 #000000 にした画像へ再書き出します。
市松模様、背景ノイズ、背景グラデーションは含めません。

これらの発光素材はサイト側で以下のように合成してください。

mix-blend-mode: screen;

ただし、blendがページ全体へ影響しないよう、ファーストビューの親コンテナへ以下を設定してください。

isolation: isolate;

台座はscreen合成を使わず、透過PNGまたは透過WebPとして通常表示してください。

UIパネルについては、黒背景画像のscreen合成だけに依存せず、
半透明のガラス面・枠・影をCSSで作り、その上へ発光線やアイコン画像をscreen合成してください。

新しい素材が揃うまでは、現在のhero-idle-background.pngをフォールバックとして維持してください。
黒背景素材と透過台座を配置したあと、DPR 2で画質と合成結果を確認し、問題がない段階で旧背景を外してください。

現在の市松模様入り素材を無理に自動除去した画像は採用しないでください。
白いモヤ、発光欠け、市松模様の残留がある状態で実装を完了しないでください。