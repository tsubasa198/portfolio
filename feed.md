ファーストビューの分解レイヤーから、スクロール動画へ切り替わるハンドオフを修正してください。

現在、分解レイヤーによる高画質な静止画から動画へ切り替わる瞬間に、ポータル・台座・マスコット・周辺装飾の位置とサイズが変わり、「パッと別映像へ切り替わった」ように見えています。

単にフェード時間を長くするのではなく、動画の先頭フレームと分解レイヤーの構図をピクセル単位で揃えてください。


# 最重要方針

動画の先頭フレームを、ハンドオフ時の唯一の正解となる基準画像として使用してください。

静止状態の分解レイヤーは自由に待機アニメーションして構いませんが、スクロールによる切り替え直前には、すべてのレイヤーを動画先頭フレームと同じ位置・サイズ・角度へ収束させてください。

以下は禁止です。

- 位置が異なる状態のままopacityだけで切り替える
- クロスフェードを長くして位置ずれをごまかす
- 動画をスライドインさせる
- 静止レイヤーと動画で異なるobject-fitを使う
- 動画開始時にマスコットやポータルが瞬間移動する
- 画面サイズごとに根拠なく個別調整値を増やす


# 1. 動画先頭フレームの抽出

実際に使用しているファーストビュー動画から、最初に表示される正確なフレームを抽出してください。

出力例：

public/assets/hero/hero-video-first-frame.webp

注意：

- 動画ファイルの0秒ではなく、ブラウザで最初に描画されるフレームを確認する
- 必要であればrequestVideoFrameCallbackで最初のデコード済みフレームを確認する
- 動画のobject-fit、object-position、scaleを適用した画面上の見え方を基準とする

この画像はサイトに常時表示するためではなく、位置合わせと検証用の基準として使用してください。


# 2. 静止レイヤーと動画を同じ座標系へ入れる

静止レイヤーと動画を、同じ親コンテナ内へ配置してください。

推奨構造：

<div class="hero-visual-stage">
  <video class="hero-transition-video" />
  <div class="hero-idle-composite">
    <!-- 背景、ポータル、台座、UI、球体、マスコット -->
  </div>
</div>

両方に以下を共通適用してください。

- position: absolute
- inset: 0
- width: 100%
- height: 100%
- 同じaspect-ratio
- 同じobject-fit
- 同じobject-position
- 同じtransform-origin
- 同じoverflow領域

動画だけcover、静止側だけ独自の100vw座標、といった別々の座標計算を使用しないでください。


# 3. 基準キャンバスを統一

分解素材の元となった構図は1672×941相当の16:9なので、静止レイヤーはこの基準座標で配置してください。

例：

const HERO_REFERENCE_WIDTH = 1672;
const HERO_REFERENCE_HEIGHT = 941;

各レイヤーの位置は、px固定ではなく基準キャンバスに対する割合として管理してください。

例：

xRatio = sourceX / HERO_REFERENCE_WIDTH
yRatio = sourceY / HERO_REFERENCE_HEIGHT

画面上では、動画に適用しているcoverまたはcontainのスケール計算と同じ式を使用して配置してください。

静止レイヤーと動画で別々のレスポンシブ計算を持たせないでください。


# 4. 動画先頭フレームとのアンカー合わせ

以下のアンカーを基準に、静止レイヤーと動画先頭フレームを一致させてください。

優先順位：

1. ポータルリングの中心座標
2. ポータルリングの横幅・縦幅
3. 台座の上面と下端
4. マスコットの中心座標
5. マスコットの足と台座の接地点
6. マスコットの表示幅・高さ
7. UIパネルの位置
8. 軌道線と前景球体

最低限、以下が切り替え前後で変化しないようにしてください。

- ポータル中心
- 台座の大きさ
- 台座の地面位置
- マスコットの顔の位置
- マスコットの足元
- ポータルとマスコットの距離

装飾粒子など細かい要素より、ポータル・台座・マスコットを優先してください。


# 5. キャリブレーション表示を実装

開発時のみ使用できる位置合わせモードを追加してください。

例：

?heroCalibration=1

または開発用フラグ。

キャリブレーションモードでは、以下を切り替えられるようにしてください。

- 分解レイヤーのみ表示
- 動画先頭フレームのみ表示
- 両方をopacity 0.5で重ねて表示
- 0.2秒間隔で交互に点滅
- 必要であればdifference相当の比較表示

両方を重ねた際に、主要要素が二重に見えなくなるまで位置を調整してください。

調整完了後、開発モード以外ではキャリブレーションUIを表示しないでください。


# 6. 待機アニメーションをハンドオフ姿勢へ収束

スクロール前は、マスコット・UIパネル・球体・ポータルに待機アニメーションがあります。

スクロールが始まった瞬間に、アニメーション途中の状態からそのまま動画へ切り替えると、動画先頭フレームと一致しません。

そのため、スクロール進捗の最初の短い区間で、待機アニメーションを停止し、各レイヤーをハンドオフ用の基準姿勢へ収束させてください。

目安：

0.000〜0.008：
- 待機アニメーションを停止
- マスコットのジャンプ・傾き・squashを通常姿勢へ戻す
- UIパネルと球体を基準位置へ戻す
- ポータル回転を動画先頭フレームの角度へ合わせる

0.008〜0.018：
- 静止レイヤーを動画先頭フレームと完全に重なる状態にする
- 動画の最初のデコード済みフレームを背面に表示
- 静止レイヤーから動画へ短時間でalphaを入れ替える

待機アニメーションをCSSループの任意の位相のまま残さないでください。


# 7. 動画は事前に描画準備する

ハンドオフ時に動画が遅れて表示されたり、一瞬黒くなったりしないようにしてください。

対応：

- preload="auto"
- muted
- playsInline
- loadedmetadata後にcurrentTimeを0へ設定
- loadeddataまたはcanplayを待つ
- requestVideoFrameCallbackが利用可能なら最初のフレーム描画を確認
- 描画準備が完了するまでは静止レイヤーを維持
- 動画のposterには抽出した先頭フレームを使用可能

動画準備前に静止レイヤーを消さないでください。


# 8. ハンドオフは位置合わせ後に短く行う

位置とサイズを一致させたあと、短いalpha入れ替えを行ってください。

目安：

- 80〜160ms相当
- 静止レイヤー：opacity 1 → 0
- 動画：opacity 0 → 1

長いクロスフェードは使用しないでください。

位置が一致していれば、短い切り替えでもユーザーには同じ画面が動き始めたように見えます。


# 9. 残る差異のみ光で吸収

静止素材と動画素材の発光・粒子が完全には一致しない場合のみ、ポータル中心にごく短い発光パルスを入れて構いません。

条件：

- 60〜120ms程度
- 白い全画面フラッシュは禁止
- ポータル中心のオレンジ発光のみ
- 位置ずれをごまかすための主手段にはしない
- 先に座標とスケールを合わせる

発光パルスは最後の微差を目立たなくする補助としてのみ使用してください。


# 10. スクロールを戻した時も同じ処理にする

逆スクロールで動画から静止レイヤーへ戻る際も、同じ先頭フレームを中継点として使用してください。

流れ：

1. 動画を先頭フレームまで戻す
2. 静止レイヤーをハンドオフ基準姿勢で重ねる
3. 短いalpha入れ替えを行う
4. 静止レイヤーへ戻ったあと、待機アニメーションを再開する

動画途中から静止待機状態へ突然戻さないでください。


# 11. レスポンシブ確認

以下の画面幅で、動画と静止レイヤーを比較してください。

- 1440×900
- 1920×1080
- 2560×1440
- DPR 1
- DPR 2
- タブレット
- スマートフォン

各サイズで以下を確認してください。

- ポータル中心がずれない
- 台座のサイズが変わらない
- マスコットの顔が移動しない
- マスコットの足元が跳ねない
- 動画へ切り替わる瞬間に画面全体がズームしない
- object-fitによるクロップ位置が一致している

PCだけに固定した補正値で解決しないでください。


# 12. 実装後の確認方法

非常にゆっくりスクロールし、切り替え前後をフレーム単位で確認してください。

必ず以下のスクリーンショットを保存してください。

- スクロール0%
- ハンドオフ直前
- 静止レイヤーと動画先頭フレームを50%ずつ重ねた状態
- ハンドオフ直後
- 動画が少し進んだ状態
- 逆スクロールで戻った直後

確認項目：

1. ポータルが二重に見えないか
2. 台座が上下左右へ動かないか
3. マスコットのサイズが変わらないか
4. マスコットの顔・足元が移動しないか
5. UIパネルが瞬間移動しないか
6. 背景のクロップ位置が変わらないか
7. 動画切り替え時に黒いフレームが出ないか
8. 逆スクロールも自然か


# 13. 完了条件

以下をすべて満たして完了としてください。

- 静止レイヤーと動画先頭フレームが同じ座標系にある
- ポータル・台座・マスコットの主要アンカーが一致している
- 待機アニメーションがハンドオフ姿勢へ自然に収束する
- 動画は事前に最初のフレームまで描画されている
- 切り替えで位置・サイズのジャンプが見えない
- 長いクロスフェードでごまかしていない
- 逆スクロールでも同じ品質で復元される
- PC・スマートフォンで破綻しない
- コンソールエラーがない
- lint、型チェック、テスト、ビルドが通る


# 完了時の報告

作業完了後、以下を報告してください。

- 動画先頭フレームの抽出方法とファイルパス
- 静止レイヤーと動画の共通座標系
- 使用したobject-fitとobject-position
- 合わせたアンカー一覧
- 修正した各レイヤーの位置・スケール
- 待機アニメーションを停止・収束させるタイミング
- ハンドオフのalpha切り替え時間
- キャリブレーションモードの使用方法
- 逆スクロール時の処理
- 確認した画面サイズ
- 実行したlint、型チェック、テスト、ビルド

まず動画の先頭フレームと現在の分解レイヤーを重ねて比較し、どの要素がどれだけずれているかを確認してから修正してください。